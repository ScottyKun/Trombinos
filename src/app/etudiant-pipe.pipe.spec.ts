import { EtudiantPipePipe } from './etudiant-pipe.pipe';

describe('EtudiantPipePipe', () => {
  it('create an instance', () => {
    const pipe = new EtudiantPipePipe();
    expect(pipe).toBeTruthy();
  });
});
